{
  "name": "mono-repo",
  "private": true,
  "packageManager": "pnpm@9",
  "scripts": {
    "dev:host": "pnpm --filter host dev",
    "build:host": "pnpm --filter host build",
    "build:app-a": "pnpm --filter app-a build",
    "build:app-b": "pnpm --filter app-b build",
    "build:all": "pnpm --filter host build && pnpm --filter app-a build && pnpm --filter app-b build"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "pinia": "^3.0.4"
  }
}
